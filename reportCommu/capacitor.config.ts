import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reportCommu.angular',
  appName: 'reportCommu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
